@page
@model Andrey.Pages.Sertificates.EditModel

@{
    ViewData["Title"] = "Редактировать сертификат";
}

<h1>Редактировать сертификат</h1>

<h4>Здесь можно поставить дату сертификата, если Вы хотите выдать его студенту.</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Sertificate.SertificateID" />
            <div class="form-group">
                <label>Студент</label>
                <select asp-for="Sertificate.StudentID" class="form-control" asp-items="ViewBag.StudentID"></select>
            </div>
            <div class="form-group">
                <label>Курс</label>
                <select asp-for="Sertificate.CourseID" class="form-control" asp-items="ViewBag.CourseID"></select>
                <span asp-validation-for="Sertificate.CourseID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Дата в сертификате</label>
                <input asp-for="Sertificate.Created" class="form-control" />
                <span asp-validation-for="Sertificate.Created" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">К списку сертификатов</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
